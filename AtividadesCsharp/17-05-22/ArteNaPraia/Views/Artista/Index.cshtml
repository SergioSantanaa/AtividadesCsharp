@model IEnumerable<ArteNaPraia.Models.Artista>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th class=>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ChavePix)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ChavePix)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdArtista">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdArtista">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdArtista">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
